# Copy this file to .env if the defaults are already taken on your machine.

# Port the Vite dev server listens on (npm run dev).
VITE_DEV_PORT=5173

# Port the browser calls the backend API on. Must match the backend's
# BACKEND_PORT (see the root .env.example) when running manually.
VITE_API_PORT=8000
